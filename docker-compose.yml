version: '3.8'

services:
  app:
    build: .
    image: flask-app
    ports:
      - "5000:5000"
    environment:
      FLASK_ENV: development
    volumes:
      - .:/app
    command: flask run --host=0.0.0.0

  tests:
    build: .
    command: ["pytest", "tests"]
